EXPECTED_VALUES="marshal|unmarshal";
PROCESS="$(get_property process)";

if ! $(is_expected_value $PROCESS $EXPECTED_VALUES); then
	echo "$MESSAGE_ERROR Invalid process!";
	exit 1;
fi;

function marshal() {
	for i in "${!ARRAY[@]}"; do
		#statements
	done
}

function unmarshal() {
}

$PROCESS
